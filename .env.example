POSTGRES_DB=
POSTGRES_USER=
# Obrigatório: use uma senha local forte. Nunca versione o arquivo .env.
POSTGRES_PASSWORD=
POSTGRES_PORT=5432

# Necessário apenas ao iniciar com: docker compose --profile dev-tools up
PGADMIN_EMAIL=
PGADMIN_PASSWORD=
PGADMIN_PORT=5050

BACKEND_PORT=8080
FRONTEND_PORT=5173

# Obrigatório: mínimo de 32 bytes UTF-8; gere um valor aleatório para cada ambiente.
JWT_SECRET=
